'use strict';

Application.Controllers.controller('authorization.error.controller', ['$scope', '$configuration', '$location', function($scope, $configuration, $location){
    var self = this;
    
    
    // SCOPE EVENT HANDLERS.
    $scope.$on("$stateChangeSuccess", function (event, next, current) {
        
    });       
  
  
    
    // CONTROLLER --------------------------------------------------------------------------------------------------------
}]);